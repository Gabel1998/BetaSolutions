version: "1.0"
linter: jetbrains/qodana-jvm:2025.1

profile:
  name: qodana.recommended

include:
  - name: CheckDependencyLicenses

exclude:
  - paths:
      - src/main/java/com/example/betasolutions/controller/**  # Exclude specific controller package and all its subpackages and files

